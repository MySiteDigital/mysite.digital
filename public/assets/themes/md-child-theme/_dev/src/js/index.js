import '../scss/main.scss';

;(
    function($) {

        jQuery(document).ready(
            function() {
                
            }
        );

        jQuery(window).on(
            'load',
            function() {
                
            }
        );

    }
)(jQuery);
